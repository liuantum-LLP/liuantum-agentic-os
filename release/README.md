# Liuant Release Artifacts

This folder stores local release metadata generated by:

```bash
./liuant release manifest
./liuant release checksum
```

`manifest.json` and `checksums.json` are safe metadata files. Native desktop artifacts, when they exist, are expected under `release/artifacts` or Tauri bundle output folders.
